# ═══════════════════════════════════════════════════════════════
#  MUST登校 · 环境变量模板
# ═══════════════════════════════════════════════════════════════
#  用法：把本文件复制成 `.env`，然后**按你的身份只填对应那一段**。
#
#  ⚠️ `.env` 已被 .gitignore 排除 —— 绝不要提交、也绝不要外发。
#  ⚠️ 本文件（.env.example）里全是**占位符**，可以安全地给人看。
# ═══════════════════════════════════════════════════════════════


# ───────────────────────────────────────────────────────────────
#  【身份 A】项目负责人 —— 有生产库密码的人
# ───────────────────────────────────────────────────────────────
# 生产库连接串。取自 Supabase 控制台：
#   Settings → Database → Connection String → 选 Transaction Pooler（端口 6543）
# 用户名形如 `postgres.项目代号`，是**超级管理员身份**，能读写删一切 —— 务必保管好。
#
# DATABASE_URL=postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres


# ───────────────────────────────────────────────────────────────
#  【身份 B】合作者 —— 用专用账号，**不是** postgres
# ───────────────────────────────────────────────────────────────
# 向项目负责人索取这串连接信息。这个账号能力受限：
#   ✅ 增删改查数据、建新表
#   ❌ 改/删现有表结构、建库、建角色、提权
# 用户名形如 `dev_partner.项目代号`。把下面那行取消注释并填上真值即可。
#
# DATABASE_URL=postgresql://dev_partner.PROJECT_REF:YOUR_PARTNER_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres


# ───────────────────────────────────────────────────────────────
#  以下两项**通常不用填** —— 它们在 Vercel 上已配置，本地不需要
# ───────────────────────────────────────────────────────────────

# 网站口令门（middleware 用它判定「是否已登录」）
#   · 线上：已在 Vercel 环境变量里配好
#   · 本地：留空即可 —— 开发环境（NODE_ENV≠production）会自动放行，调试不用输口令
# ACCESS_PASSWORD=

# 定时任务鉴权（Vercel Cron 每日调 /api/cron/rebuild 重算派生数据）
#   · 线上：已在 Vercel 环境变量里配好，Vercel 会自动以
#     `Authorization: Bearer $CRON_SECRET` 发给 cron 路由
#   · 本地：留空即可
# CRON_SECRET=


# ───────────────────────────────────────────────────────────────
#  固定值（照抄，勿改）
# ───────────────────────────────────────────────────────────────
# 澳门交通事务局报站接口（全站唯一数据源）
DSAT_BASE_URL=https://bis.dsat.gov.mo:37812/macauweb


# ═══════════════════════════════════════════════════════════════
#  📌 两个历史说明
# ═══════════════════════════════════════════════════════════════
#  1. `DATABASE_URL_LOCAL`（本地 PostgreSQL）**已于 2026-09-17 停用** ——
#     本项目现在只有云端库。脚本已全部改为「缺 LOCAL 时回退 DATABASE_URL」。
#     不要再新增这个键，除非确实要恢复本地库。
#  2. `DATABASE_URL_CLOUD_SESSION`（Supabase Session 模式直连）是**历史遗留**，
#     代码里无人读取 —— 不要新增。
# ═══════════════════════════════════════════════════════════════
