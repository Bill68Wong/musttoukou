# ① Supabase 免费额度测算

## 免费版（Free Plan）额度

| 项 | 额度 |
|---|---|
| **数据库** | **500 MB** |
| 文件存储 | 1 GB |
| 月度带宽 | 5 GB |
| 项目数 | 2 个活跃 |
（⚠️ 以上为公开资料口径，**请以您控制台实际显示为准**）

## 每行真实字节数（用 PostgreSQL 的 `pg_column_size` 实测）

- 现有 `segment_stats` 行平均 **83** 字节（11 列）
- 新表 `segment_samples` 有 **16 列**且文本字段更长 ⇒ 估 **160~200 字节/行**

### 测算（按 180 字节/行 + 索引）

| 行数 | 数据本体 | 索引（约 1.3×） | 合计 |
|---:|---:|---:|---:|
| 60,000 | 10 MB | 13 MB | **24 MB** |
| 100,000 | 17 MB | 22 MB | **39 MB** |
| 200,000 | 34 MB | 45 MB | **79 MB** |
| 500,000 | 86 MB | 112 MB | **197 MB** |
| 1,000,000 | 172 MB | 223 MB | **395 MB** |
| 2,200,000 | 378 MB | 491 MB | **869 MB** |

### ★ 关键推算：**会不会爆**

```
每轮采集约 3,000 段
若每天 2 轮 → 一年 730 轮 × 3,000 = 219 万段
219 万 × 180 字节 × 2.3 ≈ 900 MB  ✗✗ 超过 500 MB 限额！
```

⇒ ★★ **您的担心是对的** —— 全都永久留在库里，**不到一年就会爆** ✗

### ✅ 但有一个天然的解法

```
★ derived.json 文件【本来就永久存在本地】✓（data/tracking/ 已有 17 轮）
   ⇒ 数据库不需要留全部原始样本 ✓

方案：数据库只保留【最近 N 轮】的原始样本（滚动窗口）
      更早的：① 本地 json 永久存档 ② 聚合值进 segment_stats
```

| 保留轮数 | 行数 | 占用 | 评价 |
|---:|---:|---:|---|
| 10 轮 | 30,000 | **12 MB** | ✅ 很安全（<100MB） |
| 20 轮 | 60,000 | **24 MB** | ✅ 很安全（<100MB） |
| 30 轮 | 90,000 | **36 MB** | ✅ 很安全（<100MB） |
| 50 轮 | 150,000 | **59 MB** | ✅ 很安全（<100MB） |
| 100 轮 | 300,000 | **118 MB** | 🟡 可接受 |

★ **推荐：保留最近 30 轮（约 9 万行 / 40 MB）** ✓ 永远不超过 50 MB ✓
   超出 30 轮的最老轮次自动删除（原始 json 在本地仍永久保存）

### 当前生产库实际占用（对照）
- 整个数据库：**15 MB**

| 表 | 占用 |
|---|---|
| `bus_snapshots` | 904 kB |
| `dsat_call_logs` | 896 kB |
| `route_stations` | 448 kB |
| `segment_stats` | 384 kB |
| `timer_events` | 256 kB |
| `stations` | 200 kB |
| `wait_snapshots` | 144 kB |
| `lrt_timetables` | 128 kB |
| `users` | 128 kB |
| `timer_sessions` | 112 kB |

---

# ② 深入对拍：找「特别大的出入」

可比对：**205** 个站对

## 分布（看有没有「特别大的出入」）

| 区间 | 数量 | 占比 |
|---|---:|---:|
| 差 ≤10% | 81 | 40% |
| 差 10~25% | 54 | 26% |
| 差 25~50% | 60 | 29% |
| 差 50~100% | 9 | 4% |
| 🔴 差 >100%（重点） | 1 | 0% |

### 🔴 「特别大的出入」（差 >50%）：**10** 个

| 线路 | 站对 | 手动 | 手动n | 自动 | 自动n | 比值 | 可能原因 |
|---|---|---:|---:|---:|---:|---:|---|
| `51A` | T377→T359 | 0.50 | 1 | 1.62 | 81 | ×3.24 | 手动样本太少（不可信） |
| `50` | T388→T398 | 0.70 | 1 | 1.24 | 29 | ×1.77 | 手动样本太少（不可信） |
| `50` | T388→T398 | 0.70 | 1 | 1.24 | 29 | ×1.77 | 手动样本太少（不可信） |
| `59` | T418→T433 | 1.30 | 1 | 2.23 | 141 | ×1.72 | 手动样本太少（不可信） |
| `59` | T418→T433 | 1.30 | 1 | 2.23 | 141 | ×1.72 | 手动样本太少（不可信） |
| `59` | T418→T433 | 1.30 | 1 | 2.23 | 141 | ×1.72 | 手动样本太少（不可信） |
| `26` | C686→C656 | 0.80 | 1 | 1.31 | 109 | ×1.64 | 手动样本太少（不可信） |
| `26` | C686→C656 | 0.80 | 1 | 1.31 | 109 | ×1.64 | 手动样本太少（不可信） |
| `26` | C686→C656 | 0.80 | 1 | 1.31 | 109 | ×1.64 | 手动样本太少（不可信） |
| `50` | C691→C652 | 1.40 | 1 | 2.17 | 165 | ×1.55 | 手动样本太少（不可信） |

## ★ 剔除「手动样本 < 3」的格子后（更公平的对比）

- 可比对：**22** 个（手动 n≥3）
- 比值中位数：**0.959**（差 -4.1%）
- 差 ≤10%：**12**（55%）· 差 ≤25%：**16**（73%）

## 按线路分组（看有没有某条线系统性偏差）

| 线路 | 可比站对 | 比值中位 | 判读 |
|---|---:|---:|---|
| `25AX` | 9 | ×1.292 | 🔴 偏差大 |
| `26A` | 4 | ×0.711 | 🔴 偏差大 |
| `51B` | 14 | ×0.896 | 🟡 轻微 |
| `50` | 22 | ×1.067 | ✅ 一致 |
| `59` | 21 | ×1.064 | ✅ 一致 |
| `17` | 3 | ×0.948 | ✅ 一致 |
| `25` | 8 | ×0.970 | ✅ 一致 |
| `51A` | 44 | ×0.980 | ✅ 一致 |
| `26` | 33 | ×0.987 | ✅ 一致 |
| `51` | 47 | ×0.994 | ✅ 一致 |

## 结论

- 全部 205 个：比值中位 **0.989**
- 手动 n≥3 的 22 个：比值中位 **0.959**
- ⇒ ★ **无系统性偏差** ✓ 可以按「都进表 + 标注 source」的方式合并
