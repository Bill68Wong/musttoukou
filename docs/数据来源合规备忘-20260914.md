# 数据来源合规备忘 · DSAT 使用条款 / robots.txt 实测

> 实测日期：**2026-09-14**（本备忘中所有条款原文均为当日抓取）
> 目的：确认「自动采集 DSAT 报站数据」是否存在被明文禁止的情形，以及需要履行的义务。
> 探针：`.verify/robots-check.mjs` · `.verify/tos-text.mjs`（只读，输出 `.verify/robots-check.txt` / `.verify/tos-text.txt`）

---

## 一、结论速览

1. **没有任何明文禁止爬取**：API 主机 **没有 robots.txt**；`www.dsat.gov.mo` 的 robots.txt **只禁一个目录**，其余明确 `Allow: /`。
2. **条款里没有针对「自动化访问 / 爬虫 / 高频请求」的专门条款** —— 约束集中在 **版权与内容使用方式**，不在 **访问方式**。
3. 对我们的三条硬约束：
   - ① **限非商业用途**（商业用途需书面授权）；
   - ② 复制/再发布必须 **註明來源 + 獲得內容的日期**；
   - ③ **不得对内容作任何修改**（商业、非商业皆然）。
4. 派生数值另有专门条款「**引用數字**」：可以运算统计出新数字，但公布时 **必须指出来源 + 说明计算方法**。
5. 私隐声明的最关键一句：「**除非發現該等訪問對本網站進行敵意攻擊，否則不會通過這些資料去追查瀏覽者**」
   → 正常使用的访问**不会被追查**；这也与四轮采集实测（失败率 ≤0.24%、**0 次 403/429**）一致。

---

## 二、robots.txt 实测

| 主机 | 路径 | 结果 | 内容 |
|---|---|---|---|
| `bis.dsat.gov.mo:37812`（**API 主机**） | `/robots.txt` | HTTP 404 / body 为 403 页 | **不存在 robots.txt** |
| `bis.dsat.gov.mo:37812` | `/` | HTTP 200（body 403 Forbidden） | 根路径被挡，只有 `/macauweb` 应用上下文 |
| `www.dsat.gov.mo` | `/robots.txt` | HTTP 200 · text/plain · **46 B** | `User-agent: *` / `Disallow: /dsat_bres/` / **`Allow: /`** |
| `www.gov.mo` | `/robots.txt` | HTTP 200 · 373 B | 仅禁 `/wp-json/` 系列（WordPress REST），其余放行 |
| `motransportinfo.com` | `/robots.txt` | HTTP 200 · 77 B | `Disallow:`（空值）→ **全部允许** |
| `data.gov.mo` | `/robots.txt` | 返回首页 HTML | **无 robots.txt** |

**判读**：`www.dsat.gov.mo` 只屏蔽 `/dsat_bres/` 一个路径（疑似巴士报站资源目录），其余**明确写 `Allow: /`** ——
这是**极度宽松**的配置，等于公开允许抓取。API 主机不在 robots.txt 管辖范围内（无该文件）。

> ⚠️ robots.txt 缺失 ≠ 授权，只是**没有禁令**；真正的约束来自使用条款。

---

## 三、DSAT 使用條款（`www.dsat.gov.mo/dsat/subpage.aspx?a_id=1614844508`，更新日期 **2009-10-28**）

原文要点摘录（繁体原文）：

| # | 条款 | 原文关键句 |
|---|---|---|
| 1 | 版權 | 「本網站由澳門特別行政區交通事務局建立並維護，所有的內容（包括文字、圖片、聲音、影像）的版權屬交通事務局全權所有」 |
| 2 | 同意接受 | 「**當使用本網站的任何服務時，即表示同意接受本網站的使用條款和私隱聲明的約束**」 |
| 3 | 再發佈、複製 | 「除屬於協力廠商外，**可供複製並再發佈作非商業用途的使用，而無須得到交通事務局書面同意**，但**必須註明內容是由交通事務局提供和獲得內容的日期**；同時必須確保所複製的內容準確。而所複製的內容並不可代表該等資訊的正式版本，也不可作為交通事務局認可的文件」 |
| 4 | 網頁連結引用 | 「可通過網頁連結進行非商業用途的引用…但必須在引用者網頁上**註明該網頁連結是連至交通事務局網站**」 |
| 5 | 🚨 商業禁止 | 「**無論個人或機構，未得到交通事務局的書面授權，禁止利用本網站或其內容作任何形式的商業性用途**」 |
| 6 | 🚨 不得修改 | 「**無論是商業性或非商業性複製，除非得到交通事務局的書面授權，否則均不可以對內容作任何修改**」 |
| 7 | ★ 引用數字 | 「本網站有部分資料以數字形式公佈，**使用者可以對有關的數字作進一步的運算和統計而得出新的數字**。但如果要公佈這些數字，則**必須要指出其來源，並說明計算或統計的方法**」 |

**对本项目的逐条含义**：

- **#3/#4**：MUST登校是**个人非商业**项目 → 落在「非商業用途」内，**允许**复制与再发布，但**必须**：
  - 标注 **「資料來源：澳門特別行政區交通事務局（DSAT）」**；
  - 标注 **取得内容的日期**（实时页的「更新于 HH:MM」可承担此功能）；
  - 声明复制内容**不代表官方版本**（现有「仅供参考」即此意）。
- **#5**：**一旦 App 开始变现（广告 / 订阅 / 企业版），即落入禁止范围**，需先取得书面授权。这是本项目未来最重要的合规红线。
- **#6**：本项目**不修改** DSAT 原文数据（只做展示与统计），但要注意**不得改编/篡改**其站名、线路名、站序等原始内容。
- **#7（最关键）**：本项目核心卖点「还有 N 站 = 还要 X 分钟」是**对 DSAT 数字做统计运算后得出的新数字** →
  正好被这一条覆盖：**必须指出数据来源 + 说明计算/统计方法**。

---

## 四、澳門特別行政區政府入口網站 使用條款（`www.gov.mo/zh-hant/terms-of-use/`，更新日期 **2011-09-08**）

结构与 DSAT 版一致，四项：**版權宣告 / 再發佈及複製 / 引用數據 / 免責聲明**。
其中「引用數據」条款措辞与 DSAT 相同（「必須要指出其來源，以及說明你得出這些新數字的計算或統計方法」）。

⚠️ 该条款写明：「在發表修改版本後使用本網站，**包括瀏覽、下載、執行軟件、提交資料、再發佈、複製及連到本網站等均受修訂後的版本所規範**」
→ **条款可单方修改，且继续使用即视为接受新版本** → 建议**定期复检**（本备忘应设复检提醒）。

---

## 五、DSAT 私隱聲明（`...subpage.aspx?a_id=1614844510`）

自动记录的「技術資料」清单：

- 你所在的**互聯網域名及 IP 地址**
- 開始瀏覽的**日期及時間**
- 你所瀏覽的**網頁**
- 來源網站的域名

目的与门槛（原文）：

> 「收集這些訊息的目的是令本網站對你更加合用。**除非發現該等訪問對本網站進行敵意攻擊，否則不會通過這些資料去追查瀏覽者。**」

**判读**：存在访问日志（含 IP），但**追查有一个明确门槛 = 被判定为「敵意攻擊」**。
→ 常规采集不属于敌意攻击；四轮实测从未被拦截（0 次 403/429）也印证了这一点。
→ **这是「不需要换 IP」的法规侧依据**：真正会触发追查的是攻击行为，不是来源 IP。

---

## 六、本项目现状与差距

| 项 | 现状 | 差距 |
|---|---|---|
| 实时到站页来源声明 | ✅ 已有：`LiveEta.tsx`「更新于 {时刻} · 数据来自澳门交通事务局，仅供参考」 | 时刻是**抓取时刻**，可视为「獲得內容的日期」；建议措辞明确化 |
| **派生数值**（预测时长 / N→X 换算 / 方案卡） | ❌ 未发现面向用户的来源+算法声明 | **缺**「来源 + 計算/統計方法」说明（对应条款 #7） |
| **采集请求的 UA** | ❌ 实测为 `user-agent: node`（Node 默认值），无任何可识别身份 | 建议改成可识别 UA（见 §七 #1） |
| 采集脚本 | 只读、不写库、不发布 | 合规侧无需额外动作 |
| 非商业定位声明 | ❌ 未发现 | 建议在「关于」页说明「个人非商业项目」 |

---

## 七、建议动作（待主人拍板，未实施）

1. **给采集器一个可识别的 UA**（优先级最高、成本最低，改 1 行）：
   实测当前请求头为 `user-agent: node` / `accept: */*` / `accept-language: *` / 无 Referer、无 Origin
   → 对 WAF 而言这是「我是脚本」的明牌，但**无法识别是谁**。改为例如
   `MUSTDengxiao/0.27.2 (+https://musttoukou.vercel.app; personal non-commercial)`。
   价值：① 表达善意与可联系；② 若被限流，对方能识别具体应用而非野爬虫；③ 与「非商业、可追溯」自证一致。
2. **补一条派生数值声明**（对应条款 #7）：
   在展示预测时长的界面加脚注，例如
   「預測時長由歷史實測統計推算（方法：同站對相鄰區間中位數）；資料來源：澳門交通事務局；取得日期：YYYY-MM-DD；非官方數據，僅供參考」。
3. **在「關於」页补一条数据来源与用途声明**：非商业、个人项目、来源 DSAT、不修改原始内容。
4. **设一条复检提醒**（建议每季度）：条款可单方修改，且「继续使用即视为接受」。
5. **确认开放数据路径**：查 `data.gov.mo`（澳門政府數據開放平台）是否已发布交通事务局数据集；
   若有官方开放数据授权，**优先走该渠道**，合规性最强。
6. **商业化红线登记**：一旦考虑变现，**先取得 DSAT 书面授权**（条款 #5）。

---

## 八、攻击特征自查（对照四轮实测数据）

判断「是否表现为攻击」，不看意图看特征。逐项对照：

| 攻击的典型特征 | 本项目的实际表现 | 判定 |
|---|---|---|
| 尽可能快地打满带宽 / 吞吐 | **恒定 5 秒节律**，每轮固定 113 次请求（22.6 次/秒） | ✅ 不是攻击 |
| 并发拉满 | 并发上限 **6** | ✅ 不是攻击 |
| 无节制、长期持续 | 单次 **30 分钟**，且**需人工召唤**才启动、非 7×24 常驻 | ✅ 不是攻击 |
| 失败后加大强度 | **恰好相反**：失败率 >5% 自动降档 · p50 超阈值自动放宽间隔 · 连续失败 10 次**熔断退出** | ✅ 主动退让 |
| 隐藏来源 | UA = `node`（未伪装成浏览器，属「无标识」而非「伪装」） | ⚠️ 像 bot（非攻击特征） |
| 越权 / 写入 / 提交数据 | 全程**只读**，不写任何数据、不注册、不登录 | ✅ 不是攻击 |
| 目标站的判定 | 四轮合计 **0 次 403 / 429**，失败全是 `timeout`（服务端慢）与 `net:fetch failed`（本机网络断），失败率 0.0064%~0.24% | ✅ 从未被判定为攻击 |

**四条自我约束机制**（渐变启动 8→24→48→92 · 延迟降级 · 失败降档 · 硬熔断）是攻击者**绝不会**具备的行为
—— 它们的存在本身就是「温和客户端」的证据。

**结论**：
- **条款侧：合规**。个人非商业 · 只读 · 不修改原始内容 · robots.txt 未禁止 · 条款无访问方式限制。
- **攻击特征侧：不构成攻击**。恒定低频、有节流与熔断、失败率极低、目标站从未拦截。
- **唯一的「像机器人」之处**是 UA 无标识（`node`）→ 建议按 §七 #1 修正。
- **唯一未履行的条款义务**是派生数值缺「来源 + 计算方法」声明（条款 #7）→ 见 §七 #2。
- ⚠️ **需知情的一点**：token 签名算法系由官方前端 JS **逆向复刻**（见 `docs/DSAT巴士接口调研.md`）。
  该 token 是**客户端自行计算**的签名，非服务端下发的会话凭据 → 复刻它属于**模拟官方客户端行为**，
  不构成「破解访问控制」；但严格意义上仍属逆向工程范畴，故记录在案。

---

## 九、修订记录

| 日期 | 内容 |
|---|---|
| 2026-09-14 | 首版：robots.txt 实测 + DSAT／政府入口网站使用条款 + 私隐声明要点 + 现状差距与建议 |
| 2026-09-14 | 补：§六 增「采集请求 UA = `node`」差距 · §七 增 UA 建议并重排优先级 · 新增 §八「攻击特征自查」 |
