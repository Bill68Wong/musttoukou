# 全澳导航实现 · 独立验收报告

> 验收人：**严过关（QA）**　验收日期：**2026-09-18（深夜）**
> 对象：**全澳导航 v1.3.0 实现**（工作树未提交；基于 `docs/设计-全澳导航-v1-20260918.md` 修订 R5）
> 方法：**真浏览器（playwright-core + 本机 Chromium 1228）** + 扩样阴影对照 + 真实 DB/API + 旧功能回归
> 证据脚本/产物：`.verify/qa-{http,browser,browser2,browser3,shadow,miss,seg}.ts` 与同名 `.out.txt`；截图 `.verify/qa-shots/*.png`

---

## 验收结论（TL;DR）

- **总体判断：需修复后上线** —— **导航主链路（搜索→出发→出卡）可用且质量不错，但「卡片 → 详情页」这一步在真实用户流程下必崩**（P0）。修掉 P0 后即可上线。
- **P0（阻塞上线）1 个 · P1（上线前应修）4 个 · P2（可迭代）5 个**
- **最致命的一条**：点任意一张卡进 `/nav/detail` → **服务端异常（应用错误页）**。根因是 Server Component 把**函数** `colorOf` 当 prop 传给 Client Component `RouteStack`（RSC 不能序列化函数）——`next build` 通过、`tsc` 0 错误、`GET /nav/detail` 也返回 200，**但真实浏览器点卡即错**（决定性复现见 §①/§④）。

> 先说好的（都在主链路）：**两段式搜索**（打字纯本地 0 配额 / 回车走高德）按 R5 落地并带「来自高德」徽章 ✓；**车程走 `lookupHop` 逐跳实测**（抽查 25 路 5 跳全 L1 实测）✓；**步行 = 高德几何 ÷84**（278m→3.3 分）✓；**卡片数按实际显示**（home→school=3 张、school→gate=2 张，不凑 5）✓；**middleware 白名单已正确加**（公众可访问）✓；**旧功能未破** ✓；首页 0 console error ✓。

---

## 问题清单（按严重度）

| # | 类别 | 问题 | 证据（截图/数字/命令） | 影响 | 建议 |
|---|---|---|---|---|---|
| **P0-1** | 正确性/崩溃 | **`/nav/detail` 服务端异常**：`<RouteStack colorOf={colorOf}>` 把**函数**从 Server Component 传给 Client Component | ① 浏览器点卡 → `Application error: a server-side exception`，Digest **3136894847**；② server log：`Error: Functions cannot be passed directly to Client Components … {codes:["25"], colorOf: function x}`；③ 独立复现：`GET /nav/detail?…` 三种模式（plain/RSC/Router-State）**全部**在响应体含该序列化错误（`.verify/qa-rsc.txt`）；④ `/nav/detail/page.tsx:97,113` + `RouteStack.tsx:1 "use client"` | **核心流程「看详情」100% 不可用** | 改成传**可序列化的 `colors: Record<string,string>`**（或把构造 colorOf 的逻辑放进 client 子组件）；`RouteStack` 已支持 `colors`？若无则加一路；修后回归「任意卡→详情」 |
| **P1-1** | 数据/引擎 | **「漏线率」被硬上限放大、且缓解方案不足以达标** | 我扩到 **32 OD**、4 种配置（`.verify/qa-shadow.out.txt`）：**A(k8,cap80，=engineer 同口径)=72.9%** → B(k8,cap**2000**)=**53.5%** → C(k16)=**34.1%** → D(k40/3km，近似含高德站点)=**26.4%**；且 **32/32 OD 全部「触顶 80」** | ① engineer 报的 58.3% **偏乐观**（5 个校园 OD）；真实更差；② 仅把 `maxCandidates` 从 80 抬高就降 19pp ⇒ 该数字**大半是 cap 假象**；③ **即使 D 配置（含高德选中站）仍 26.4% > 10%** ⇒「把高德选中站并入」**不足以**压到 10% 以下 | 把 `maxCandidates` 抬高（≥2000）并**显式统计触顶率**；把「漏线率」定位为**降级引擎完整性指标**（非门禁）；单独统计含 `__amap_`（映射失败）的链 |
| **P1-2** | 一致性(⑥) | **`/nav` 卡片区文案是繁体，与页面级简体混排** | /nav DOM 同时含简体「3 条路线 / 刷新 / 步行 / 预计」与繁体「上車 / 下車 / 行車 / 點擊看詳情 / 預計 / 條路線 / 無實時車輛」；来源 `RecommendCards.tsx:74-76`（复用旧组件） | 违反 R5「界面文案一律简体」；且与设计「呈现与旧版一致」**自相矛盾** | 产品需二选一：① 把复用组件的界面词改简体；② 明确「卡片区保留繁体、仅新页壳简体」并写入设计 |
| **P1-3** | 设计/实现不一致 | **短距修正系数文档写 ×1.5，实现在用 ×2.5** | 设计 §C.2「`直线<200m → 直线×1.5`」；实现 `walk-fix.ts`：`min(高德, 直线×2.5)`，且短距 `ratio≤3` 时**直接用高德值**（`ANOMALY_CLAMP_FACTOR=2.5`，文件头自述「R3 改过」） | 后人照文档改代码会引入回归 | 把设计 §C.2 回写为最终规则（×2.5 / 短距 ratio≤3 保留高德） |
| **P1-4** | 降级路径 | **降级出卡的「本地候选」首末步行未按设计走 `walk_cache`/`直线×1.5`** | `nav-service.ts:317` 本地候选用 **`baseCtx`（无 `walkResolver`）** → `model.ts#walkView` 落到旧 `walkIdx`（GPS/POI 起点 → 全局均值 level4 / 常数 3.0） | 降级态（高德不可用）步行精度**低于设计承诺**（§B.6） | 降级态为本地候选注入「`walk_cache`（geohash7）→ 未命中 `直线×1.5`」的 resolver |
| **P2-1** | 稳健性 | **transit 超时预算 1500ms 偏紧** | 实测 warm `transitMs=864ms`（已用掉 58%）；冷启动 + 跨洲网络易超 1.5s | 冷启动下可能频繁误判降级 | 冷启动首调放宽（如 3s）或重试一次 |
| **P2-2** | 诚实性 | **「（列出均為實時結果，不含估算）」与卡片内 L5/L6 估算段矛盾** | home→school 卡 2（N5）`levels=[4,4,4,6,4,6,4]`（含 L6 估算），但页面横幅写「不含估算」 | 轻微误导 | 横幅文案按实际估算占比动态化，或沿用旧口径并在文档注明 |
| **P2-3** | 验证缺口 | 详情页**全页 GET 返回的正文未含折叠栏/线路标签** | 直接 `GET /nav/detail?…` 正文含 `返回路线`/`rc-detail` 但**不含** `本站台`/`route-stack` | 需在修 P0 后回归「流式是否真的渲染出折叠栏与站条」 | 修 P0 后加 e2e 断言：详情页必须含 `.route-stack` 与折叠栏 |
| **P2-4** | 代码质量 | build 4 条 lint 警告（含 `nav-service.ts:217 odKey` **死变量**、`poi-search.ts:86` **no-unused-expressions**） | `.verify/qa-build.out` | 轻微；`poi-search.ts:86` 的无用表达式可能是笔误 | 清理 |
| **P2-5** | 一致性(⑥) | `/about` 页仍大量繁体（既有），本次只新增披露段 | `.verify/qa-diag3.txt` | 非本次回归；与新页壳简体风格不一致 | 后续统一 |

---

## 六项验证的逐项结果

### ① 真浏览器验证（含截图路径）

**环境**：`next build`（exit 0，45s，仅 4 lint 警告）→ `next start -p 3939` → playwright-core 1.62.1 + 本机 Chromium 1228（真浏览器，非 HTTP mock）。控制台错误 **0**。

| 步骤 | 结果 |
|---|---|
| `/` 首页 | ✅ 标题「澳门出行」、旧功能通道「通勤计时（旧版）›」、2 个搜索框、合规「高德」均在 |
| **地图** | ✅ **加载成功**：`<script src=webapi.amap.com>` 已注入、`window.AMap=object`、NavMap `phase=ready`、**无「地图暂不可用」占位**（`AMAP_JS_KEY=408b…` 已在 `.env`；`AMAP_JS_SECURITY_CODE` 未配，但未触发失败占位） |
| 打字（本地） | ✅ 输入「澳科大」→ **1 候选（source=local，0 配额，52-365ms）**；输入「威尼斯人」→ 本地站点候选 + pending；输入「lrt」→ 返回轻轨站清单 |
| 回车（高德） | ✅ 输入错别字「澳科打」回车 → **4 候选，全部带「来自高德」徽章**，首条「澳门科技大学」 |
| 点「出发」→ `/nav` | ✅ 跳转 `/nav?...`，出 **3 张卡**（骨架→卡片）；赶车档文案（全力冲刺/小跑/正常走/慢慢走/爬过去）在 |
| **点卡片 → `/nav/detail`** | 🔴 **失败**：`Application error: a server-side exception … Digest: 3136894847`（= **P0-1**） |
| `/commute` 旧首页 | ✅ 正常（「MUST登校 選方向 → 立刻看到最快路線」+ 4 个方向卡） |
| 地图失败降级 | ✅ **已隐式验证**：本机历史上缺 securityJsCode 时地图服务受限，但搜索/出卡/详情（除 P0）**照常**；且 NavMap 代码路径自带失败占位 + 重试 |

**截图（`.verify/qa-shots/`）**：`01-home.png`、`02-type-local.png`、`03-enter-amap.png`、`04-ready.png`、`06-nav-cards.png`、`b2-01-home-mapfail.png`、`b2-03-nav.png`、`b2-04-detail.png`（错误页）、`b3-old-card.png`、`08-_commute.png`、`08-_recommend.png`。
> ⚠️ 本会话模型无法读图，故以上以**DOM 文本断言**为准；截图为留证。

### ② 阴影对照扩样（≥30 OD · 漏线率真实数字）

- **扩到 32 OD**（锚点：home/school/gate/hengqin/T317(氹仔码头)/M134(市中心)/T322/LRT-MUST/C690；含校园走廊/口岸/氹仔/跨区）。**凌晨 01:29**，1 个 OD（school→LRT-MUST）高德 0 方案已跳过并标注「深夜样本不足」。
- **真实漏线率（Σmiss÷Σ高德组合）**：

| 配置 | 漏线率 | 本地路径合计 | 触顶 |
|---|---:|---:|---|
| A `k=8, cap=80`（= engineer 同口径） | **72.9%** | 2480 | **32/32 全触顶** |
| B `k=8, cap=2000` | **53.5%** | 62000 | 全触顶 |
| C `k=16, cap=2000` | **34.1%** | 62000 | 全触顶 |
| D `k=40, radius=3km`（近似含高德站） | **26.4%** | 155000 | 全触顶 |

- **口径复核结论（独立）**：
  1. **engineer 的 58.3% 是真实计算，但样本偏小且被上限放大**：其脚本 `shadow-diff.ts` 里 `searchStationPaths(..., {maxCandidates: 80})` 是**硬编码**，而实测 **每个 OD 都触顶 80** ⇒ 枚举在「0 换乘」或「1 换乘」中途被截断，大量合法组合从未生成。
  2. **仅把 cap 从 80 抬到 2000**，漏线率从 **72.9%→53.5%（−19pp）** ⇒ 该数字**约六成是 cap 假象**。engineer 的根因分析（「高德选的 C653 是第 9 近站 ⇒ 被 K=8 截掉」）**部分成立但不完整**——cap 同样是主因。
  3. **评估缓解方案「把高德选中的上下车站并入本地候选」**：用配置 D（k=40/3km，足以覆盖高德选中的站）近似 ⇒ 仍 **26.4% > 10%**。**⇒ 该缓解不足以把漏线率压到 10% 以下**；残余来自 ① 链中含 `__amap_`（站码映射失败，永不匹配）② 本地有界 BFS 的 hub 限量（25）与「中段=两 hub 共有线」约束。
- **重要边界**：**主路径交付的是高德方案本身（漏线=0）**，此指标只反映 **降级引擎完整性 + 监控信号**。故 P1-1 的实际影响是「降级质量 + 监控阈值不可用」，而非主路径缺路线。

### ③ 2 个残留 miss 的复核结论

**结论：不是脚本/映射 bug，是真实「高德路径 vs 用户实际捷径」的口径差（且样本极小）。**

| 例 | 直线 | 高德 | ratio | 实测 | 折算(高德÷84) | 隐含速度 | 判读 |
|---|---:|---:|---:|---:|---:|---:|---|
| home→C653 | 228.0m | 309m | 1.36 | 2.8 分 (n=13) | 3.7 分 (+31%) | 1.84 m/s(高德) / 1.36(直线) | 高德 ratio **正常**（∈[1,3]→按规则保留），但路径比人走的捷径长 ⇒ 规则无法识别 |
| school→T400 | 474.3m | 716m | 1.51 | 6.5 分 (n=**1**) | 8.5 分 (+31%) | 1.84 / 1.22 | 同上；且实测样本仅 1，噪声大 |

- 两例 ratio 都落在「正常区间」，**任何规则都不该改**（改了会犯 ×1.5 误伤 38% 的旧错）⇒ 属**固有残差**。
- 附带发现（供校准口径修正）：全表 44 行「折算 vs 实测」中位 ratio **0.90**，`±20%` 命中仅 **21/44**；但该统计**未按 zone 对齐**（把 R 座实测 2.8 分错配到 966m），故偏低是假象；`calibrate-walk.ts` 的 zone-aware **90.0%（18/20）** 更可信。
- **裁定**：engineer 的「属高德路网 vs 实测口径差」判断**正确**，接受为已知残差；建议把两例写入「非 bug 清单」。

### ④ 端到端真实链路（含与 segment_stats 的抽查对比）

- 真实坐标（place_coords WGS84→GCJ）调 `/api/nav`（擎天匯→澳科大）：

```
count=3 degraded=false transitOk=true transitFromCache=false
stats={staticMs:644, transitMs:864, amapPlans:4, bridged:4, dropped:[],
       localCandidates:19, localKept:0, localSource:"offline", liveMs:64, modelMs:3, ms:1995}
卡1 25路 total=26min walkOut=3.3min(lv1) ride=10.3min levels=[1,1,1,1,1] tier5"爬过去都能赶上"
卡2 N5  total=29.3  walkOut=7(lv1)     ride=15.2min levels=[4,4,4,6,4,6,4] tier4
卡3 25→N5 total=31.4
```
- **数字合理性**：`walkOut 3.3 分 = 278m÷84` ✓；`walkIn 6.5 分 = 549m÷84` ✓；`totalMs=1995`（含 transit 864ms）。
- **抽查 `segment_stats`**：卡1 的 25 路 `levels=[1,1,1,1,1]` = 每一跳都命中 **L1（本线·今天·stop）实测**；我按 `route_code='25'` 抽查库内分层行（`.verify/qa-seg.out.txt`）：`C653→C692 wk=-1 stop p50=2.2 n=62`、`T363/2→T306 p50=1.8 n=62` 等，**样本量充足**；今天(weekday=6)层亦有行。⇒ **卡片车程确来自 `segment_stats` 实测回退链，非估算、非高德时长** ✓。
- **负向确认**：`localKept=0`（本地补漏被质量闸门全数挡下，未污染主列表）✓；`GET /api/nav` 无 `planId` 时 `count` 随实际（3/2），符合「不足 5 按实际显示」。

### ⑤ 旧功能回归（逐项）

| 项 | 结果 |
|---|---|
| `/commute`（旧首页） | ✅ 浏览器渲染正常（方向卡 ×4） |
| `/recommend` | ✅ 正常（「擎天匯 → 澳科大（N/O 座）· 最快 2 條」+ 卡片） |
| `/card`（页面） | ✅ 可达（200）；`/recommend` 与 `/card` 同形渲染 |
| `/api/recommend`（旧接口） | ✅ 200，返回 `cards`（walkOut 2.8 分 lv1 **samples=13**、ride 25 10.3 分、tier5） |
| `/records`、`/stats`、`/timer/1` | ⚠️ **被口令门挡住**（`ACCESS_PASSWORD` 未配 → fail-closed 重定向 `/login`）——这是 **v1.1.10 既有行为**，非本次回归；本地无口令无法逐页验收 |
| **`RouteStack` 是否被改（换标签风险）** | ✅ **未改**：`git status` 显示改动文件**不含** `src/components/RouteStack.tsx`（`.verify/qa-git.txt`）⇒ 旧 `/card` 标签样式不受影响 |
| `model.ts` 改动是否波及旧路径 | ✅ 代码为**新增可选字段** `ModelContext.walkResolver`（无则走原 `walkIdx`，旧链路零变化）；`/api/recommend` 实跑正常佐证 |
| `middleware.ts` 改动 | ✅ 仅**新增白名单**（`/nav`、`/nav/detail`、`/commute`、`/api/nav`、`/api/poi/suggest`、`/_AMapService`）——必要且正确 |
| 构建 | ✅ `next build` exit 0；新增路由 `/nav`、`/nav/detail`、`/commute`、`/api/nav`、`/api/poi/suggest`、`/api/amap-service` 均产出 |

### ⑥ 一致性检查（文案简繁 / 标签规则）

- **文案简繁**：🔴 **不达标（P1-2）**。`/nav` 页面级简体（`NavResultClient.tsx:81`「3 条路线」），但**卡片区繁体**（`RecommendCards.tsx:74-76`「目前僅 N 條路線可用」「（列出均為實時結果，不含估算）」、`上車/下車/行車/點擊看詳情/預計`）→ **混排**。
- **站名/线路名 = 繁体**：✅（卡片与站条显示 `C653 金峰南岸/金譽峰`、`連貫公路/威尼斯人` 等繁体原名）。
- **线路标签规则**：✅ 新导航**统一用 `.route-stack`**（`RouteStack` 组件）：搜索下拉的线路候选用它、卡片标题线路链用它；`.route-chip` 仅出现在旧 `TimerWizard`（不在新导航），**未违反**「≥2 线用 stack」。
- **轻轨文案禁「地铁」**：✅ 代码 `uiLineLabel()` 把高德 `地铁线路`→「轻轨」（未在本轮样本命中，属静态检查）。
- **卡片配色**：卡片沿用中性底（`--surface`），未见主题色背景撞色。

---

## 我实际做了什么（命令 + 真实输出）

| 我做的事 | 命令/方法 | 结果 |
|---|---|---|
| 生产构建 | `npm run build` | exit 0，`Compiled successfully`，4 lint 警告 |
| 起生产服务 | `npm start`（PORT=3939） | Ready 672ms；`GET /`→200 |
| **真浏览器全流程** | `node … /qa-browser.ts`（playwright-core + Chromium 1228） | 首页/打字/回车/出发/出卡 OK；**点卡→详情=应用错误(P0)**；0 console error；截图 4+ 张 |
| **浏览器 DOM 深查** | `node … /qa-browser2.ts` | 地图 `phase=ready`+AMap 脚本已加载；线路候选 `.route-stack`×4；/nav 简繁混排取证；点 `.rc` → `/nav/detail` 报错 |
| **旧 /card 回归** | `node … /qa-browser3.ts` + `git status` | /recommend 卡片正常；`RouteStack.tsx` 未在改动清单 |
| **HTTP 真实链路** | `node … /qa-http.ts`（真实坐标调 `/api/nav` 等） | home→school count=3 / school→gate count=2；stats 全量；旧 `/api/recommend` OK；`/api/plans`→401（口令门） |
| **阴影对照扩样** | `node … /qa-shadow.ts`（32 OD ×4 配置） | A=72.9%→D=26.4%；32/32 触顶；缓解不足以 <10% |
| **残留 miss 复核** | `node … /qa-miss.ts`（walk_times×station_walk_distance + 直线） | 两例均为真实口径差；全表中位 ratio 0.90 |
| **segment_stats 抽查** | `node … /qa-seg.ts` | 25 路走廊分层行样本充足（p50,n）；唯一物理邻接对 886 |
| **确定性复现 P0** | `GET /nav/detail?…` ×3 模式（plain/RSC/Router-State） | **全部响应体含 `colorOf` 序列化错误** |

---

## 因深夜/缺凭据无法验证的部分（诚实）

1. **实时报站的真实性**：验收在 **01:2x（澳门凌晨）**，DSAT 夜班车稀少；本轮卡片确实拿到了实时（`还有 N 站 · 约 …`），但**无法覆盖高峰/收班/跨零点时刻表**等时序场景。
2. **高德 JS 底图的「图面是否正确」**：本会话模型**不能读图**，只能断言「脚本已加载、`window.AMap` 存在、未走失败占位」；**瓦片是否正确渲染未目视确认**。且 `AMAP_JS_SECURITY_CODE` 未配，服务类请求（定位等）可能受限。
3. **旧数据页 `/records` `/stats` `/timer`**：`ACCESS_PASSWORD` 未配 → 生产 fail-closed，**未逐页真机验收**（可用设口令的服务重跑）。
4. **降级路径的端到端**：未对「高德不可用」注入故障做实跑（仅静态读到 `degraded` 分支与 P1-4 的 resolver 缺口）。
5. **纯步行方案 / `too_close` / 收班空状态**：代码已实现，但凌晨样本未自然触发，未做真机验证。

## 附：测试写入说明（生产库）
- 仅写 **`transit_cache`**（正常缓存）与 **`search_quota_log`**（真实搜索 1~2 次，正常记账）；**未写** `shadow_diff_report`（该表 5 行来自 engineer 的 `shadow-diff --apply`，是 `nav-service` 补漏读取的生产数据，**请勿清理**）。
- 新增脚本与截图均在 `.verify/qa-*`（可复跑）。
